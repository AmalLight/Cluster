
sudo apt install -y nodejs npm

sudo npm install -g npm

sudo npm cache clean -f

sudo npm install -g n

sudo n stable

sudo npm uninstall -g cloudcmd gritty
sudo npm install   -g cloudcmd gritty

# cloudcmd --port 8666 --terminal --terminal-path `gritty --path` --save
